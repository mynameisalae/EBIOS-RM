"""The three intake validation cases — identical / document-only / contradiction (conception §11)."""

import pytest


@pytest.mark.skip(reason="pending mission_context.validation implementation")
def test_contradiction_is_never_auto_resolved():
    pass
