"""Fiche de test — validate_extraction (conception §5.3)."""

import pytest


@pytest.mark.skip(reason="pending Fact model implementation")
def test_extraction_without_source_quote_is_rejected():
    # fact = Fact(field_name="edr_av_deploye", value="CrowdStrike Falcon",
    #             origin="extraction", source_quote="")
    # assert validate_extraction(fact) is False
    pass
