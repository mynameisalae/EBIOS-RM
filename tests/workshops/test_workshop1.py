"""Fiche de test — Atelier 1 (conception §15)."""

import pytest


@pytest.mark.skip(reason="pending workshop 1 implementation")
def test_baseline_scope_decisions_never_empty_and_rgpd_split():
    # w1_output = run_workshop_1({
    #     "mission_context": {"facts": [...], "applicable_frameworks":
    #         ["ISO27001", "ANSSI_hygiene", "RGPD", "NIST"]}
    # })
    # assert w1_output["baseline_scope_decisions"]  # never empty
    # assert all(e["covers_risk_category"] == [] for e in rgpd_art30_type_entries)
    # assert all(e["covers_risk_category"] != [] for e in rgpd_art32_type_entries)
    pass
