"""Workshop 1 — Cadrage et socle de sécurité (conception §15).

Single agent. Consumes the Mission Context only, never raw documents.
Extracts essential/support assets, feared events with gravity, and assesses
the security baseline against the declared frameworks. Also runs the
dedicated legal-impact assessment (§15.1), independent of baseline gaps.
"""
