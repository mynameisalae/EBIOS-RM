"""Workshop 4 — Scénarios opérationnels, fan-out / fan-in (conception §18).

The only multi-agent workshop: N independent Agno Agent instances, never
Team, one per strategic scenario, dispatched via asyncio.gather. Baseline
gaps arrive stripped of framework/control_id (conception §12.3, §15) — this
workshop only ever sees the raw weakness, never its regulatory origin.
"""
